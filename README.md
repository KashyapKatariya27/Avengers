# Avengers travel landing page

This is a static HTML, CSS, and JavaScript site. To deploy it on Vercel, import this repository and use the default settings: no build command and no output directory are needed. Vercel serves `index.html` from the project root.
